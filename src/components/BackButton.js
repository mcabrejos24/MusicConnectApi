export default function BackButton() {
    return (
        <div className="btn-container">
            <a href={ "/" }>
                <button className="back-btn">BACK</button>
            </a>
        </div>
    )
}
