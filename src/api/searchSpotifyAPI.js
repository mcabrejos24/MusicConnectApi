export default function containsPlaylistSpotify(playlistName) {
    // console.log('searching for playlist in spotify');
    //authValue referenced in here with the playlistName being passed through api path
    
    return false // will be true if contains false if not
}